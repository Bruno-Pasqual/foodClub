describe("Cadastro de usuário", () => {beforeAll(() => {
    //Lógica que será executada antes de todos os testes uma única vez
    'use strict';

});
afterAll(() => {
    //Lógica que será executada após todos os testes
    
});
beforeEach(() => {
    //Lógica que será executada antes de cada teste
});
afterEach(() => {
    //Lógica que será executada depois de cada teste
});

    it ("Deve retornar erro se email for vazio", ()=>{

        let x = 4;

        expect(x).toBe(2);

    })
});