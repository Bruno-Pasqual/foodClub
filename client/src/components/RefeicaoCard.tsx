type Props = {
	id: string; // Agora, cada prato tem um ID
	name: string;
	description: string;
	price: number;
	image: string;
};

const RefeicaoCard = (props: Props) => {
	return <div>RefeicaoCard</div>;
};

export default RefeicaoCard;
